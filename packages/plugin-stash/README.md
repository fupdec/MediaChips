# Stash import plugin

Imports scenes, performers, studios, tags, and markers from a Stash `stash-go.sqlite` database into the active MediaChips library.

Enable under **Settings → Plugins**. The import panel appears on the same tab.
