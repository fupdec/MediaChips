/**
 * @deprecated Import from `@/plugins/adult/hostBridge` or `@mediachips/plugin-adult`.
 * Kept as a thin re-export so existing relative imports keep working.
 */
export {
  ADULT_COMPONENT_KEYS,
  adultHostComponentMap as adultComponentMap,
  adultPlugin,
} from './hostBridge'
