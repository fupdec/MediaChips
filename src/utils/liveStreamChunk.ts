/** @deprecated Prefer importing from @shared/liveStreamChunk. */
export {
  LIVE_STREAM_CHUNK_SECONDS,
  LIVE_STREAM_CHUNK_HANDOFF_SECONDS,
  getChunkStart,
  getChunkDuration,
  getLiveStreamOutputDuration,
  getNextChunkStart,
  getContinuousNextChunkStart,
  resolveLiveFileDuration,
} from '@shared/liveStreamChunk'
